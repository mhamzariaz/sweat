import axios from "axios";

export const saveTeachingSchedule = async (data) => {
    // const response = await axios.post();
    // if(response){
    //     alert("Submitted Successfully!")
    // }
}

export const saveAssessments = async (data) => {
    // const response = await axios.post();
    // if(response){
    //     alert("Submitted Successfully!")
    // }
}

export const getSimulations = async (params = "") => {
    // const response = await axios.get();
    // if(response){
    //     return response
    // }
}